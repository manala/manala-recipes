version: "3.7"

services:

  ##############
  # Kubernetes #
  ##############

  kubernetes:
    hostname: kubernetes
    build:
      context: .
      target: kubernetes
    network_mode: bridge # Use default docker bridge network
