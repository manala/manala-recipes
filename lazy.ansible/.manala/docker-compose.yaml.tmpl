version: "3.7"

services:

  ###########
  # Ansible #
  ###########

  ansible:
    hostname: ansible
    build:
      context: .
      target: ansible
    network_mode: bridge # Use default docker bridge network
