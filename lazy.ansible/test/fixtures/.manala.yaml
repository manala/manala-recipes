manala:
    repository: ..
    recipe: lazy.ansible

project:
    name: test

system:
    docker: true
    goss:
        version: 0.3.21
    ssh:
        config: |
            # Ssh config
    ansible:
        version: 2.14.2
        config: |
            # Ansible config
    ansible-lint:
        version: 6.12.1
    molecule:
        version: 4.0.4
        plugins:
            version: 23.0.0
            azure: true
            docker: true
            gce: true
