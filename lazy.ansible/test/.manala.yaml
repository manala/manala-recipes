manala:
    repository: ../..
    recipe: lazy.ansible

project:
    name: lazy-ansible-test

system:
    docker: true
    goss:
        version: 0.3.21
    git:
        config: |
            # Git config
    ssh:
        config: |
            # Ssh config
    ansible:
        version: 2.14.3
        config: |
            # Ansible config
    ansible-lint:
        version: 6.14.2
    molecule:
        version: 4.0.4
        plugins:
            version: 23.0.0
            azure: true
            docker: true
            gce: true
