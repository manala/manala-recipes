manala:
    repository: ../..
    recipe: lazy.ansible

project:
    name: lazy-ansible.test

system:
    env:
        TEST: test
    env_file:
        - .env.1
        - .env.2
    docker: true
    goss:
        version: 0.4.2
    apt:
        packages:
            - jq
    git:
        config: |
            # Git config
    ssh:
        config: |
            # Ssh config
    ansible:
        version: 2.15.5
        config: |
            # Ansible config
        dependencies:
            - hvac==1.2.1
    ansible-lint:
        version: 6.14.6
        dependencies:
            - pytest==7.4.2
    molecule:
        version: 5.0.1
        dependencies:
            - molecule-plugins==23.5.0
            - molecule-plugins[docker]==23.5.0
