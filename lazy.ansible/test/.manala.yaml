manala:
    repository: ../..
    recipe: lazy.ansible

project:
    name: lazy-ansible-test

system:
    docker: true
    goss:
        version: 0.3.21
    apt:
        packages:
            - jq
    git:
        config: |
            # Git config
    ssh:
        config: |
            # Ssh config
    ansible:
        version: 2.14.3
        config: |
            # Ansible config
        dependencies:
            - hvac==1.1.0
    ansible-lint:
        version: 6.14.2
        dependencies:
            - pytest==7.2.2
    molecule:
        version: 4.0.4
        dependencies:
            - molecule-plugins==23.0.0
            - molecule-plugins[docker]==23.0.0
