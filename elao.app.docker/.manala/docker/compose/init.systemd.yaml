services:

    #######
    # App #
    #######

    app:
        build:
            args:
                MANALA_INIT: systemd
        cgroup: host
        tty: true
        tmpfs:
            - /run
            - /run/lock
        volumes:
            - /sys/fs/cgroup:/sys/fs/cgroup:rw
