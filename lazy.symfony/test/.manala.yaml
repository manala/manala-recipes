manala:
    repository: ../..
    recipe: lazy.symfony

project:
    name: lazy-symfony-test

system:
    locale: en_US.UTF-8
    env:
        TEST: test
    env_file:
        - fixtures/.env.1
        - path: fixtures/.env.2
        - path: fixtures/.env.3
          required: false
    mount:
        - fixtures/mount_relative
        - fixtures/mount_native:/fixtures/mount_native
    tmpfs:
        - /tmpfs
        - /tmpfs/exec:exec
    docker: true
    apt:
        packages:
            - goss
            - nano
    git:
        config: |
            # Git config
    nginx:
        version: 1.28
    php:
        version: 8.3
        extensions:
            - bcmath
    nodejs:
        version: 22
    mariadb:
        version: 10.5
    phpmyadmin:
        version: 5.2.1
    maildev:
        version: 2.1.0

##########
# Deploy #
##########

deploy:
    inventory:
        foo: {}
    includes: []
    excludes: []
    shared_paths: []
    shared_files: []
    hooks:
        before_symlink: []
